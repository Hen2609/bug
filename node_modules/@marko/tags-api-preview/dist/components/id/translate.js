var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target, mod));
var import_compiler = require("@marko/compiler");
var import_wrapper_component = require("../../transform/wrapper-component");
var import_assert_no_assignments = __toESM(require("../../util/assert-no-assignments"));
module.exports = function translate(tag) {
  const tagVar = tag.node.var;
  const errorMessage = !tagVar ? "requires a tag variable" : !import_compiler.types.isIdentifier(tagVar) ? "does not a destructured tag variable" : tag.node.attributes.length ? "does not support attributes" : tag.node.arguments ? "does not support arguments" : tag.node.body.params.length ? "does not support tag body parameters" : tag.node.body.body.length ? "does not support body content" : void 0;
  if (errorMessage) {
    throw tag.get("name").buildCodeFrameError(`The <id> tag ${errorMessage}.`);
  }
  (0, import_assert_no_assignments.default)(tag.get("var"));
  const meta = (0, import_wrapper_component.closest)(tag.parentPath);
  tag.replaceWith(import_compiler.types.variableDeclaration("const", [
    import_compiler.types.variableDeclarator(tagVar, import_compiler.types.callExpression(import_compiler.types.memberExpression(meta.component, import_compiler.types.identifier("elId")), [import_compiler.types.stringLiteral(`@${meta.refIndex++}`)]))
  ]));
};
