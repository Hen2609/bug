var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target, mod));
var import_compiler = require("@marko/compiler");
var import_is_at_root = __toESM(require("../../util/is-at-root"));
const usedTag = /* @__PURE__ */ new WeakSet();
function buildInputExpr(prop) {
  return import_compiler.types.memberExpression(import_compiler.types.identifier("input"), import_compiler.types.identifier(prop));
}
module.exports = (tag) => {
  var _a;
  const errorMessage = usedTag.has(tag.hub) ? "can only be used once within a template" : tag.node.var ? "does not support a tag variable" : !(0, import_is_at_root.default)(tag) ? "can only used at the root of the template" : !tag.node.attributes.length ? "requires a default attribute" : tag.node.body.body.length ? "does not support body content" : tag.node.body.params.length ? "does not support tag body parameters" : ((_a = tag.node.arguments) == null ? void 0 : _a.length) ? "does not support arguments" : void 0;
  if (errorMessage) {
    throw tag.get("name").buildCodeFrameError(`The <return> tag ${errorMessage}.`);
  }
  usedTag.add(tag.hub);
  const props = [];
  for (const attr of tag.get("attributes")) {
    if (attr.isMarkoAttribute()) {
      props.push(import_compiler.types.objectProperty(import_compiler.types.stringLiteral(attr.node.name), attr.node.value));
    } else {
      props.push(import_compiler.types.spreadElement(attr.node.value));
    }
  }
  const returnInput = buildInputExpr("_return");
  tag.replaceWith(import_compiler.types.expressionStatement(import_compiler.types.logicalExpression("&&", returnInput, import_compiler.types.callExpression(returnInput, [
    import_compiler.types.objectExpression(props),
    import_compiler.types.numericLiteral(1)
  ]))));
};
