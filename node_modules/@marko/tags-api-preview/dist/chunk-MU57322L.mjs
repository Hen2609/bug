import {
  init_taglib_id,
  taglibId
} from "./chunk-45C4GXDQ.mjs";
import {
  __esm
} from "./chunk-XYON6ZHC.mjs";

// src/util/is-core-tag.ts
import { getTagDef } from "@marko/babel-utils";
function isCoreTag(name, tag) {
  if (tag.isMarkoTag()) {
    const def = getTagDef(tag);
    if (def && def.name === name && def.taglibId === taglibId) {
      return true;
    }
  }
  return false;
}
var init_is_core_tag = __esm({
  "src/util/is-core-tag.ts"() {
    init_taglib_id();
  }
});

export {
  isCoreTag,
  init_is_core_tag
};
