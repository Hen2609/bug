import {
  __esm
} from "./chunk-XYON6ZHC.mjs";

// src/util/assert-no-assignments.ts
var assert_no_assignments_default;
var init_assert_no_assignments = __esm({
  "src/util/assert-no-assignments.ts"() {
    assert_no_assignments_default = (bindingPath) => {
      for (const name in bindingPath.getBindingIdentifiers()) {
        const binding = bindingPath.scope.getBinding(name);
        const [assignment] = binding.constantViolations;
        if (assignment) {
          throw assignment.buildCodeFrameError("Reference is a constant and cannot be assigned.");
        }
      }
    };
  }
});

export {
  assert_no_assignments_default,
  init_assert_no_assignments
};
