import {
  __commonJS
} from "../../chunk-XYON6ZHC.mjs";

// src/transform/hoist-tag-vars/index.ts
var require_hoist_tag_vars = __commonJS({
  "src/transform/hoist-tag-vars/index.ts"(exports, module) {
    module.exports = function hoist(_owner, name, hoister) {
      let initialized = false;
      return (child, val) => {
        if (child) {
          initialized = true;
          hoister(val);
        } else if (!initialized) {
          throw new ReferenceError(`Cannot access '${name}' before initialization`);
        }
      };
    };
  }
});
export default require_hoist_tag_vars();
