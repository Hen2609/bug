var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target, mod));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var tag_body_parameters_exports = {};
__export(tag_body_parameters_exports, {
  default: () => tag_body_parameters_default
});
module.exports = __toCommonJS(tag_body_parameters_exports);
var import_compiler = require("@marko/compiler");
var import_babel_utils = require("@marko/babel-utils");
var import_is_api = __toESM(require("../util/is-api"));
var tag_body_parameters_default = {
  MarkoTag(tag) {
    const body = tag.get("body");
    const params = body.get("params");
    const tagDef = (0, import_babel_utils.getTagDef)(tag);
    if ((0, import_is_api.default)(tag, "class") || !params.length || (tagDef == null ? void 0 : tagDef.translator)) {
      return;
    }
    for (const name in body.getBindingIdentifiers()) {
      const binding = body.scope.getBinding(name);
      if (binding) {
        const [assignment] = binding.constantViolations;
        if (assignment) {
          throw assignment.buildCodeFrameError("Cannot assign to tag body parameters in the tags api preview.");
        }
      }
    }
    body.node.params = [
      import_compiler.types.assignmentPattern(import_compiler.types.objectPattern([
        import_compiler.types.objectProperty(import_compiler.types.identifier("default"), import_compiler.types.assignmentPattern(import_compiler.types.arrayPattern(body.node.params), import_compiler.types.arrayExpression([])))
      ]), import_compiler.types.objectExpression([]))
    ];
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {});
