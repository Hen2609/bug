var import_track_rendering = require("../../translate/track-rendering");
module.exports = function createElGetter(owner, key) {
  return owner[key] || (owner[key] = () => {
    if ((0, import_track_rendering.isRendering)()) {
      throw new Error("Cannot read an element reference while rendering.");
    }
    return owner.getEl(key);
  });
};
