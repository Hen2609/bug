import {
  __commonJS
} from "../../chunk-XYON6ZHC.mjs";

// src/translate/native-tag-handlers/index.ts
var require_native_tag_handlers = __commonJS({
  "src/translate/native-tag-handlers/index.ts"(exports, module) {
    var ignoreReg = /^on[A-Z]|Change$/;
    module.exports = (from) => {
      const attrs = {};
      for (const key in from) {
        if (!ignoreReg.test(key)) {
          attrs[key] = from[key];
        }
      }
      return attrs;
    };
  }
});
export default require_native_tag_handlers();
