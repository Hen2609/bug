const ignoreReg = /^on[A-Z]|Change$/;
module.exports = (from) => {
  const attrs = {};
  for (const key in from) {
    if (!ignoreReg.test(key)) {
      attrs[key] = from[key];
    }
  }
  return attrs;
};
